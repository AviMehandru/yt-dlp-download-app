# Cargo's build directory. Reaches several GB after a release build, and
# every byte of it is regenerated by `cargo build`.
src-tauri/target/

# Generated by tauri-build at compile time from tauri.conf.json and the
# capabilities/ files -- not a source.
src-tauri/gen/
